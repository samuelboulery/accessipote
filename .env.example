# Variables d'environnement pour Accessipote

# Mode de développement
# NODE_ENV peut être 'development' ou 'production'
# Ne pas modifier manuellement, géré automatiquement par Vite
# NODE_ENV=production

# Configuration API (si vous ajoutez une API backend)
# API_URL=http://localhost:3000/api

# Configuration analytics (exemple pour Google Analytics)
# VITE_GA_ID=

# Configuration Sentry pour le tracking d'erreurs (optionnel)
# VITE_SENTRY_DSN=

# Autres variables d'environnement
# Pour ajouter d'autres variables, préfixez-les avec VITE_
# Elles seront accessibles via import.meta.env.VITE_*
